In playground: 
1. Add a column to the right of the xml DSL editor, show supported xml tags and attributes, and their descriptions.
2. make the vertical divider between the xml DSL editor and the supported tags/attributes column draggable, so users can adjust the width of each section according to their preference.
3. default to 2/3 width for the xml DSL editor and 1/3 width for the supported tags/attributes column, but allow users to resize as needed.