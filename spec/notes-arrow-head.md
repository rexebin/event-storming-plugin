When an element's next or altNext is a note element, the arrow head should be on the source element, not the note, 
indicating that the note is attached to the source element, not the target element.
This is a common convention in event storming diagrams to visually indicate that a note is describing or 
annotating the source element rather than the target element.